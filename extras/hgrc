[ui]
# Name data to appear in commits
username = Ryan C. Scott <ryan@5pmCasual.com>
ssh = "c:\Program Files (x86)\PuTTY\plink.exe" -ssh -2 -batch -C

[extensions]
record =
shelve = 
graphlog =
color =

[merge-tools]
#emacsclient.args = -q --eval "(require 'ediff)" --eval "(setq ediff-window-setup-function 'ediff-setup-windows-plain)" --eval "(add-hook 'ediff-quit-hook 'save-buffers-kill-emacs)" --eval "(ediff-merge-with-ancestor \""$local"\" \""$other"\" \""$base"\" nil \""$output"\")"

[alias]
cdiff = diff --color=always
cstat = stat --color=always
cglog = glog --color=always
clog = log --color=always

force-merge-other = merge --tool internal:other
force-merge-mine = merge --tool internal:local

[color]
status.modified = cyan bold
status.added = green bold
status.removed = yellow bold
status.deleted = red bold
status.unknown = magenta bold
status.ignored = black bold

diff.diffline = bold
diff.extended = magenta bold
diff.file_a = green bold
diff.file_b = green bold
diff.hunk = magenta
diff.deleted = yellow
diff.inserted = cyan
diff.changed = white
diff.trailingwhitespace = bold white_background
